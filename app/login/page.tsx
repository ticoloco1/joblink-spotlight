import Login from '@/views/Login';

export const metadata = {
  title: 'Login | JobinLink',
  description: 'Entre na sua conta JobinLink',
};

export default function LoginPage() {
  return <Login />;
}
